# Copy to .env and fill in real values (never commit .env)

MONGO_URL=mongodb://localhost:27017
DB_NAME=smartstock_db
SECRET_KEY=change-me-in-production

# --- Email (Gmail example) — required for low-stock + test email ---
# If you see "535 Username and Password not accepted": you MUST use an App Password,
# not your normal Gmail password. 2-Step Verification must be ON first.
# https://myaccount.google.com/apppasswords
MAIL_USERNAME=mantriharshita2004@gmail.com
# No quotes around values. Spaces in app password are OK; avoid trailing spaces on the line.
MAIL_PASSWORD=vmgv wkzx etzx xhut
MAIL_FROM=mantriharshita2004@gmail.com
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587

# Optional
# FRONTEND_URL=http://localhost:5173
# OPENAI_API_KEY=
# Twilio SMS (optional): TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_FROM_NUMBER, LOW_STOCK_SMS_TO
